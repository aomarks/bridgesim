const GRID_SIZE = 61;
const TILE_PX = 10;
const BLIP_PX = 2;
const HP = 0.5;  // half pixel

// Target frames per second;
const TGT_FPS = 60;

// Target milliseconds per frame.
const TGT_MPF = 1000 / TGT_FPS;

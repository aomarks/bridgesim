namespace Bridgesim.Client {
  export const HP = 0.5;              // half a pixel
  export const SIM_TICK = 1000 / 30;  // milliseconds per simultation tick
  export const NET_TICK = 1000 / 30;  // milliseconds per network tick
}
